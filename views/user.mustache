<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <title>Document</title>
    <link rel="stylesheet" href="../css/main.css" />
</head>

<body>
    <div class="container">
        <table>
            <tr>
                <td>
                    <img id="avatar" src="{{avatar}}" />
                </td>
                <td class="leftCol">
                    <h1>{{name}}</h1>
                    {{#address.city}}
                    <span>{{address.city}}, {{address.country}} | </span> {{/address.city}} {{^address.city}}
                    <span> {{address.country}}</span> {{/address.city}} {{#job}}
                    <span class="position">{{.}} {{company}}</span> {{/job}} {{^job}}
                    <span class="forHire" style="color:red">Available for hire</span> {{/job}}
                    <span>{{email}} | {{phone}} </span>
                </td>
            </tr>
            <tr>
                {{#university}}
                <td class="rightCol">
                    <span class="bold">Education </span>
                </td>
                <td class="leftCol"> <span>{{.}}</span>
                </td>
                {{/university}}
            </tr>
            <tr>
                <td class="rightCol">
                    <span class="bold">Skills </span>
                </td>
                <td class="leftCol">
                    <ul class="skillList">
                        {{#skills}}
                        <li>{{.}}</li>
                        {{/skills}}
                    </ul>
                </td>
            </tr>
        </table>
    </div>
</body>

</html>